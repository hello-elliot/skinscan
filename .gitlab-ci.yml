stages:
  - deploy

pages:
  stage: deploy
  image: alpine:3.20
  before_script:
    - apk add --no-cache bash
  script:
    - sh scripts/build_pages.sh
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH'
